groups:
  - name: node-alerts
    rules:
      - alert: InstanceDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Server {{ $labels.name }} is DOWN"
          description:  "{{ $labels.name }} ({{ $labels.instance }}) is unreachable"
